<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <script>
            <%- sessionData ? "window.sessionData = JSON.parse(atob('" + btoa(JSON.stringify(sessionData)) + "'));" : "" %>
            <%- initialData ? "window.initialData = JSON.parse(atob('" + btoa(JSON.stringify(initialData)) + "'));" : "" %>
        </script>
    </head>
    <body>
        <div id="root" style="position: absolute; left: 0px; top: 0px; width: 100%; height: 100%;"></div>
        <script src="<%- scriptPath %>"></script>
    </body>
</html>